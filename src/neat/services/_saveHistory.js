export async function _saveHistory(db, rowsGlobal) {
    console.log('JSON.stringify(rowsGlobal) = ',JSON.stringify(rowsGlobal))
    console.log('10 rowsGlobal.length = ',rowsGlobal.length)
    // есть весь список скачанный по нему вычисляем новых удаленных и их дату из sitemap
}

